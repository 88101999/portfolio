<div class="container">
  <h1 class="text-center mb-4">おすすめのコーディネート</h1>

  <div class="row">
    <% if @coordinates.present? %>
      <% @coordinates.each do |coordinate| %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div id="coordinate-card-<%= coordinate.id %>" class="card h-100 coordinate-card" data-bs-toggle="modal" data-bs-target="#coordinateModal<%= coordinate.id %>">
            <% if coordinate.image.attached? %>
              <%= image_tag coordinate.image, class: "card-img-top", alt: coordinate.name %>
            <% else %>
              <!-- 画像がない場合のプレースホルダー -->
              <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 250px;">
                <span class="text-white">画像なし</span>
              </div>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= coordinate.name %></h5>
            </div>
          </div>
        </div>

        <%= render 'coordinates/modal', coordinate: coordinate %>
      <% end %>
    <% else %>
      <div class="col-12">
        <p class="text-center text-muted">コーディネートがありません</p>
      </div>
    <% end %>
  </div>
</div