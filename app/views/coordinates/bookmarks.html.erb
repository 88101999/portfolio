<% content_for :title, 'お気に入り' %>
<div class="container pt-3">
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @bookmarks.present? %>
          <%= render @bookmarks %>
        <% else %>
          <p>お気に入りのコーディネートがありません。</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
